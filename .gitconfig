[alias]
	# Shortcuts
	a = add
	s = status
	d = diff
	c = checkout
	cm = commit -m

[apply]
	# Detect whitespace errors when applying a patch
	whitespace = fix

[merge]
	# https://ductile.systems/zdiff3/
	conflictstyle = zdiff3

[core]
	# Treat spaces before tabs and all kinds of trailing whitespace as an error
	# [default] trailing-space: looks for spaces at the end of a line
	# [default] space-before-tab: looks for spaces before tabs at the beginning of a line
	whitespace = space-before-tab,-indent-with-non-tab,trailing-space
	# Include my global gitignore
	excludesfile = ~/.gitignore_global

[diff]
	# Detect copies as well as renames
	renames = copies
	# https://luppeng.wordpress.com/2020/10/10/when-to-use-each-of-the-git-diff-algorithms/
	algorithm = histogram

[fetch]
	# Clean up removed branches when fetching/pulling
	prune = true
	pruneTags = true
	all = true

[help]
	# Don't autocorrect typos, suggest an alternative instead
	autocorrect = prompt

[pager]
	diff = diff-so-fancy | less --tabs=1,5 -RFX
	show = diff-so-fancy | less --tabs=1,5 -RFX

[pull]
	rebase = true

[push]
	# https://git-scm.com/docs/git-config#git-config-pushdefault
	default = simple
	# Make `git push` push relevant annotated tags when pushing branches out.
	followTags = true
	autoSetupRemote = true

[status]
	short = true
	branch = true
	showUntrackedFiles = all

[init]
	defaultBranch = main

[branch]
	# Sort branches by when their last commit was made
	# This is more useful than the default sorting by name
	sort = -committerdate

[tag]
	forceSignAnnotated = true
	sort = version:refname

[user]
	name = Bart Veneman
	# email = TODO

[github]
	username = bartveneman

[column]
	ui = auto

[rerere]
	enabled = true
	autoupdate = true
